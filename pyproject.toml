[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-predictive-maintenance"
version = "0.1.0"
description = "ML + RAG + AI agent predictive maintenance copilot"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

